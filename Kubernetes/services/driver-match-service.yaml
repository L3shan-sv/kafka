apiVersion: v1
kind: Service
metadata:
  name: driver-match-service
  namespace: rideflow
spec:
  type: ClusterIP
  selector:
    app: driver-match-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000